{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/filcuk/shared/main/dir/links.schema.json",
  "title": "Shared links catalog",
  "description": "Topic-grouped catalog of sites, repositories, packages, and other links.\n\nConventions:\n- Topics are domains (what it's about).\n- type describes where the primary url takes the user (destination), not the software kind—e.g. store listing, package registry, repo, CLI/TUI release, or web app.\n- tags are optional extras not already implied by topic or type (e.g. wearable, iot, physible).\n- languages are optional Simple Icons slugs for primary languages/tooling (e.g. javascript, python).\n- Array order is display order.\n- Icon/accent paths without a scheme are resolved relative to the item url (join against url as base).\n- Skip topics/items with hidden: true unless explicitly showing drafts/examples.\n- featured marks items eligible for highlight surfaces; omit when false.\n- repo is the source repository when it differs from url.",
  "type": "array",
  "minItems": 1,
  "items": {
    "$ref": "#/$defs/topic"
  },
  "$defs": {
    "topic": {
      "type": "object",
      "additionalProperties": false,
      "required": ["topic", "items"],
      "properties": {
        "topic": {
          "type": "string",
          "minLength": 1,
          "description": "Domain grouping (e.g. Data & Analytics, Other, Meta)."
        },
        "hidden": {
          "type": "boolean",
          "description": "When true, consumers should omit this entire topic from normal listings (e.g. schema examples)."
        },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/item"
          }
        }
      }
    },
    "item": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "label", "url", "type"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "description": "Stable kebab-case slug. Prefer keeping this unchanged if the label or url moves."
        },
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "Display name."
        },
        "subtitle": {
          "type": "string",
          "description": "Short one-line blurb shown next to the label."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Longer Markdown body. Omit when unused."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Primary target URL for the link."
        },
        "repo": {
          "type": "string",
          "format": "uri",
          "description": "Source repository URL when it differs from url—e.g. a Pages site, package page, releases page, or store listing."
        },
        "icon": {
          "$ref": "#/$defs/assetRef",
          "description": "Default (light) icon. Absolute URI, or path relative to url."
        },
        "iconDark": {
          "$ref": "#/$defs/assetRef",
          "description": "Optional dark-mode icon override. Absolute URI, or path relative to url."
        },
        "accent": {
          "$ref": "#/$defs/color",
          "description": "Default (light) brand accent colour."
        },
        "accentDark": {
          "$ref": "#/$defs/color",
          "description": "Optional dark-mode accent override."
        },
        "type": {
          "$ref": "#/$defs/itemType",
          "description": "Where url takes the user (single primary destination)."
        },
        "tags": {
          "type": "array",
          "description": "Optional cross-cutting labels not already implied by topic or type. Omit when empty.",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          }
        },
        "languages": {
          "type": "array",
          "description": "Optional primary language/tooling icons for display. Use Simple Icons slugs (e.g. javascript, python, typescript, csharp). Consumers typically render https://cdn.simpleicons.org/{slug}. Omit when empty or not meaningful.",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          }
        },
        "featured": {
          "type": "boolean",
          "description": "When true, eligible for highlight/featured surfaces. Omit when false."
        },
        "hidden": {
          "type": "boolean",
          "description": "When true, consumers should omit this item from normal listings. Omit when false."
        }
      }
    },
    "itemType": {
      "type": "string",
      "description": "Primary url destination. progressive-web-app / static-web-app: hosted web app; cli / tui: CLI or TUI distribution (e.g. releases); package: registry page; store: app/add-on store listing; repository: source repo; profile: personal/profile page.",
      "enum": [
        "progressive-web-app",
        "static-web-app",
        "website",
        "cli",
        "tui",
        "app",
        "package",
        "store",
        "repository",
        "profile"
      ]
    },
    "assetRef": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute http(s) URI, or a path relative to the item url (no scheme/host)."
    },
    "color": {
      "type": "string",
      "pattern": "^#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6}|[0-9A-Fa-f]{8})$",
      "description": "CSS hex colour (#RGB, #RRGGBB, or #RRGGBBAA)."
    }
  }
}
